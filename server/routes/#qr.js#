import express from 'express';
import QRCode from 'qrcode';

const {qr_create } = require(../controller/qr_controller.js);

const router = express.Router();

router.post('/create', qr_create);

module.exports = router;
